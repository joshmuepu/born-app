import React from 'react'
import ReactDOM from 'react-dom/client'
import ProjectionApp from './ProjectionApp'
import './ProjectionApp.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ProjectionApp />
  </React.StrictMode>
)
